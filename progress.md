# Precision Marketing Auto - Progress

## 2026-03-11

### 今日完成
- 恢复并稳定了自动化主脚本，完成多次回退与修复，确保第1/2/3步主流程可执行。
- 修复第3步执行员工关键问题：
  - 小窗口/缩放导致的视口外点击问题。
  - 默认区域残留清理逻辑（含“全国”双击清空）。
  - 可见面板绑定与状态回读修复（避免读到隐藏面板）。
- 修复第2步 iframe 早执行问题：
  - 增加“关键控件就绪”等待，避免空壳 iframe 导致主消费营运区和券规则ID未命中。
- 调整第3步策略为“不区分渠道切换”，统一按当前页面字段填充，避免“渠道未命中”干扰。
- 更新测试数据 `data/plans.csv` 为未来时间，避免“发送时间不能选历史时间”拦截。
- 完成可视化UI增强（已在之前提交）：
  - 支持下载 Excel 模板与 UTF-8 BOM CSV 模板。
  - 支持上传 `.xlsx` 并自动转 `.csv`。
  - 脚本读取 CSV 改为 `utf-8-sig`，兼容 Windows Excel。
- 安装了 `chrome-devtools` 技能（来自 `ChromeDevTools/chrome-devtools-mcp`）用于后续调试。

### 已打稳定标记
- `stable-cb0fe65-known-good-20260306`
- `stable-e2e-pass-20260306-employee-fixed`

### 当前状态
- 代码已推送到 `main`，最新提交包含：
  - `12d393e`（第3步入口强校验）
  - `0b72824`（测试数据时间更新）

### 下一步计划
- 用最新 `data/plans.csv` 进行一轮完整回归（第1/2/3步 + 保存）。
- 重点验证第2步在不同网络波动下的 iframe 就绪稳定性。
- 若通过，新增一个“生产发布候选标签”（stable tag）供全国推广回退。
- 继续优化可视化UI：
  - 增加业务“通过/失败判定清单”提示。
  - 增加第2步失败的可视化诊断提示（就绪状态、缺失控件项）。

### 本轮新增（渠道多选）
- 脚本新增参数：`--step3-channels`（支持多选，逗号分隔）。
- CSV 新增可选列：`channels`（例如：`会员通-发客户消息,会员通-发客户朋友圈`）。
- 第3步改为按渠道判断：
  - 选中“会员通-发客户消息”才强制填写/校验“短信内容”。
  - 未选短信渠道时，短信内容自动跳过，不再记失败。
- 第3步等待时间放宽，降低“加载慢导致未进入第3步”的误判。
- 可视化 UI 新增“第3步渠道多选”复选项，并透传到脚本执行。
- 渠道字段映射规则已落地：
  - 选择“短信” => 仅填写“短信内容”。
  - 选择“会员通-发客户消息” => 填写“结束时间 / 执行员工 / 发送内容”。
  - 选择“会员通-发客户朋友圈” => 填写“结束时间 / 执行员工 / 发送内容 / 朋友圈图片”。
  - 多选时按并集执行；未选择时按页面自动识别兜底。

### 2026-03-16 补充
- 测试时间已统一更新为：
  - 计划时间：`2026-03-16 08:00` 到 `2026-03-27 08:00`
  - 发送时间：`2026-03-20 08:00`
- 已同步文件：
  - `precision-auto-playwright-batch.py` 默认测试数据
  - `data/plans.csv`
  - `data/ui-test/plans-moments-local2.csv`

### 本轮新增（朋友圈图片上传）
- 第3步新增朋友圈图片上传逻辑（渠道=会员通-发客户朋友圈）：
  - 新增 CSV 字段：`moments_add_images`（是/否）、`moments_image_paths`（多图路径，`|` 分隔）。
  - 当 `moments_add_images=是` 时，脚本会逐张上传图片（按路径顺序，最多9张）。
  - 上传前校验：仅 `jpg/png`，单张小于 10MB，文件必须存在。
  - 当 `moments_add_images=否` 时，自动跳过图片上传。
- UI 模板（CSV/XLSX 下载）同步增加上述字段，并添加业务提示文案。
- UI 直接上传朋友圈图片能力：
  - 上传页新增“朋友圈上传图片 + 图片文件选择”控件（无需手工改本地CSV路径）。
  - 勾选后会把图片保存到 `ui_uploads/<task_id>_images/`，并自动回写任务CSV：
    - `moments_add_images=是`
    - `moments_image_paths=本地保存路径（按上传顺序）`
  - 这样业务只需要在UI上传 CSV + 图片即可执行，不需要手工在企业页面点文件夹。

### 本轮新增（Windows 一键使用）
- 新增 Windows 一键启动脚本：`scripts/windows/windows_start_ui.bat`
  - 自动创建 `.venv`
  - 自动安装 `requirements.txt` + `requirements-ui.txt`
  - 自动执行 `playwright install chromium`
  - 自动打开 `http://127.0.0.1:8790`
- 新增桌面图标生成脚本：`scripts/windows/create_desktop_shortcut.ps1`
  - 一次执行后在桌面生成“精准营销自动化工具”快捷方式
- 新增 Windows 打包脚本：`scripts/windows/build_windows_exe.bat`
  - 在 Windows 上可打包 `dist/PrecisionMarketingUIStarter.exe`
- README 已新增 Windows 使用说明，便于发给非技术执行同事。
- 新增“单文件下载包”输出：
  - 脚本：`scripts/windows/build_windows_release_zip.py`
  - 产物：`release/precision-marketing-auto-windows-oneclick.zip`
  - 同事可直接下载 zip，解压后双击 `scripts/windows/windows_start_ui.bat`。

---

## 维护约定
- 每次会话结束前更新本文件：
  - 今天做了什么
  - 完成了哪些
  - 下一步做什么
- 关键可回退版本必须同步写入“已打稳定标记”。

---

## 2026-03-16 进展补充

### 今天处理了什么
- 修正第3步渠道规则为最终版：
  - `短信` -> 仅填 `短信内容`
  - `会员通-发客户消息` -> 填 `结束时间 / 执行员工 / 发送内容`
  - `会员通-发客户朋友圈` -> 填 `结束时间 / 执行员工 / 发送内容 / 朋友圈图片`
- 将测试数据时间改为：
  - 计划时间：`2026-03-16 08:00:00` 到 `2026-03-27 08:00:00`
  - 发送时间：`2026-03-20 08:00:00`
- 修正第1步业务校验识别：
  - 若下一步后仍停留在第1步，优先识别 `发送时间不能选历史时间` 等提示，不再误判为第2步/第3步问题。
- 修正第2步完成后的“下一步”点击逻辑：
  - 点击后若仍停留在第2步，会自动补点一次。
- 修正第3步加载判定：
  - 识别文本、placeholder、`contenteditable` 编辑器，避免“已到第3步但脚本误判未进入”。
- 修正第3步 `会员通-发客户消息` 场景下的字段定位兜底：
  - `结束时间` 优先按可见 placeholder 定位。
  - `发送内容` 优先按可见编辑器定位，不再强依赖外层 `.item` 文案。
  - `执行员工` 优先按可见 cascader 输入框定位，不再强依赖“执行员工”标签块。

### 当前已完成
- 代码修改已落地到：
  - `precision-auto-playwright-batch.py`
- 语法检查已通过：
  - `python3 -m py_compile precision-auto-playwright-batch.py`

### 下一步
- 继续验证 `会员通-发客户消息` 第3步是否能稳定填入：
  - `结束时间`
  - `执行员工`
  - `发送内容`
- 若仍失败，优先根据新日志继续精修第3步控件定位，而不是回退整套流程。

### 结果更新（本轮）
- `会员通-发客户消息` 全流程已回归成功。
- 关键修复：
  - 第3步就绪判定从“宽松（placeholder/contenteditable）”改为“强信号命中（至少2项）”。
  - 第3步执行员工选择去除 `scroll_into_view_if_needed` 依赖，改为点击 + JS 事件兜底，规避元素不稳定超时。
  - 第3步按 `channels` 先切渠道页签再填字段，避免停留在默认渠道导致字段不可见。
  - 第3步（会员通-发客户消息）新增“添加小程序”自动化：
    - 点击“添加小程序”拉起弹窗
    - 配置小程序选择（默认：大参林健康）
    - 填写小程序标题、页面路径
    - 上传小程序封面（本地文件）
    - 点击弹窗确认

## 2026-03-17 进展补充

### 今天处理了什么
- 完成业务端 UI 调整：
  - 去掉“操作人”输入项。
  - 新增本地缓存（任务列表、日志、渠道勾选、高级配置参数）。
  - 优化第1/2/3步布局和对齐（高级配置按钮同排、渠道默认不勾选会员通消息、字段对齐）。
  - 第3步“小程序链接”文案与提示统一。
- 模板体验优化：
  - 下载 CSV/XLSX 模板改为中文表头，便于业务直接填写。
  - 上传时自动把中文表头映射回脚本英文字段，兼容旧逻辑。
- Windows 安装与升级链路落地：
  - 重写 `scripts/windows/windows_start_ui.bat`（稳定检查 Python、自动建 venv、依赖安装打标记、启动 UI）。
  - 新增 `scripts/windows/windows_install_or_update.ps1`（从 GitHub Release 拉取最新 zip 并安装/覆盖更新）。
  - 新增 `scripts/windows/install_or_update_from_github.bat`（业务双击安装/更新入口）。
  - 新增 `scripts/windows/windows_update.bat`（已安装目录内一键更新）。
  - 更新 `scripts/windows/build_windows_release_zip.py`，将新脚本打入发布包并更新快速说明。
  - 更新 `README.md` 的 Windows 使用说明，改为“安装/启动/更新”标准路径。

### 当前已完成
- 新发布包已可生成：`release/precision-marketing-auto-windows-oneclick.zip`
- 发布包已包含：
  - `scripts/windows/windows_start_ui.bat`
  - `scripts/windows/windows_update.bat`
  - `scripts/windows/install_or_update_from_github.bat`
  - `scripts/windows/windows_install_or_update.ps1`
- Python 语法检查通过：
  - `ui_app/server.py`
  - `scripts/windows/build_windows_release_zip.py`
  - `precision-auto-playwright-batch.py`

### 下一步
- 在 1 台 Windows 业务机走一遍真实安装流程（仅双击安装脚本），确认首次启动耗时与失败率。
- 将 Release 页面固定为“最新稳定包下载入口”（便于全国同事统一获取）。
- 增加“启动前环境自检页面提示”（Chrome/CDP/端口占用）以降低支持成本。

## 2026-03-17 进展补充（第2步与模板）

### 今天处理了什么
- 第2步目标分群增强：
  - 支持主消费营运区路径写法（例如：`华中大区-江西省区-九江`），自动按路径展开并定位到末级节点勾选。
  - 若页面未出现“选择数据”控件，则主消费营运区自动跳过，不阻断流程。
- 第2步“券规则ID”增强：
  - 若页面未出现券规则ID行，自动跳过，不阻断流程。
- 第2步“其他字段缺失自动跳过”增强：
  - 更新方式字段缺失时自动跳过。
  - 预跑按钮缺失时自动跳过。
  - 严格模式下校验项改为按“配置了且页面存在/可操作”动态校验，不再固定强拦。
- UI 模板下载字段精简（业务版）：
  - 模板已去掉以下列：`分群名称`、`第3步渠道(可多选)`、`朋友圈是否上传图片`、`朋友圈图片路径(用|分隔)`、`会员通消息是否添加小程序`、`小程序名称`、`小程序标题`、`小程序封面路径`、`小程序链接`。
  - 上传仍兼容这些字段（若文件里有，系统可识别），只是模板不再默认暴露。
- 新增测试文件：
  - `data/ui-test/plans-step2-area-path.csv`（用于验证“华中大区-江西省区-九江”路径选择）。

### 当前已完成
- 代码已提交并推送：`e202fdb`
- 语法检查通过：
  - `precision-auto-playwright-batch.py`
  - `ui_app/server.py`

### 下一步
- 用 UI 页面导入 `plans-step2-area-path.csv` 实测第2步路径勾选。
- 若线上页面结构再变，继续补充“节点文本命中 + 可见性 + 兜底点击”策略。

## 2026-03-18 进展补充（创建链接策略 + 业务网页入口）

### 今天处理了什么
- 创建链接策略升级：
  - 新增组合渠道规则：当第3步同时选择 `短信` + `会员通-发客户消息` 时，自动使用：
    - `https://precision.dslyy.com/admin#/marketingTemplate/use?useId=600035736992907264`
- 新增“手动创建链接”能力（优先级最高）：
  - 支持 CLI 参数：`--create-url`

## 2026-03-20 进展补充（社群单页联调）

### 今天处理了什么
- 针对社群链接（`addcommunityPlan`）第1步/第2步流程做了单页兼容修复：
  - 第1步没有“下一步”按钮时不再直接失败，改为按单页流程继续。
  - `--skip-step2` 模式下，社群单页不再强制点击“下一步”。
- 修复第1步下拉选择高频问题（复用到全渠道）：
  - 下拉查找改为**双向滚动**（先回顶部向下，再从底部向上）。
  - 下拉未命中时，自动“重开下拉再查一次”。
- 修复“营销主题”多选异常：
  - 选择前先清理历史已选标签，避免默认值叠加导致误选变多。
  - 回读去重并过滤 `+N` 汇总标签，避免日志出现 `其他 + 3 其他 + 3` 这类脏值。

### 你反馈并已记录的现象
- 社群测试日志中出现：
  - `场景类型=会员营销` 未命中（页面仍为`促销营销`）。
  - `计划类型=会员权益` 未命中（页面仍为`促销精准营销（大促&会员日）`）。
- 该问题仍需继续验证页面下拉可见项与实际可选值是否受联动限制（场景类型→计划类型）。

### 当前状态
- 已完成代码修复并通过脚本加载校验（`run_path`）。
- 待你下一轮回归确认：
  1. 营销主题是否稳定仅命中 `其他、会员生日礼`；
  2. 社群单页在 `--skip-step2` 下是否不再因“下一步”失败；
  3. 场景类型/计划类型联动是否命中 `会员营销 -> 会员权益`。

### 下一步
- 若仍未命中`场景类型/计划类型`：
  - 增加“失败时打印该字段真实可见下拉项（仅失败打印）”；
  - 增加“场景类型切换后等待计划类型选项刷新”专门重试。
  - 支持 CSV 字段：`create_url`
  - 优先级：手动创建链接 > CSV create_url > 组合渠道规则 > 单渠道规则 > 默认链接。
- 业务网页版（第1步）新增“创建链接”输入框：
  - 可手动填写创建链接，上传任务时透传到脚本执行。
  - 增加动态提示文案：根据勾选渠道显示当前自动匹配的 useId（含组合渠道提示）。
  - 本地缓存已纳入 `create_url` 字段，刷新后保留。

### 当前已完成
- 代码改动已完成并通过语法检查：
  - `precision-auto-playwright-batch.py`
  - `ui_app/server.py`

### 下一步
- 用业务网页版实测以下场景：
  1) 只选短信 -> 自动匹配短信链接。
  2) 只选会员通-发客户消息 -> 自动匹配会员通消息链接。
  3) 同时选短信+会员通-发客户消息 -> 自动匹配组合链接 `600035736992907264`。
  4) 手动填写创建链接 -> 应覆盖自动匹配结果。

## 2026-03-19 进展补充（郑州/大郑州营运区命中修复）

### 今天处理了什么
- 修复第2步“主消费营运区”简称命中问题：
  - 新增区域别名标准化：`郑州 -> 大郑州营运区`。
  - 多选解析后统一做别名归一，避免 CSV 写简称导致 not_found。
  - 在第2步树选择提示链中补充了郑州路径：
    - `郑州/大郑州营运区 -> 西北大区 -> 河南省区`。
- 修复第3步“执行员工”深层节点勾选问题：
  - 执行员工目标同样增加别名归一（含加盟后缀场景）。
  - 选择逻辑改为“路径优先 + 多层菜单兜底”：
    - 支持在 menu1~menu4 尝试勾选；
    - 对 `广佛省区`、`大郑州营运区` 及其 `加盟` 节点增加显式路径提示；
    - 未命中时遍历大区展开后再次勾选，提升小窗口/渲染延迟下命中率。

### 当前已完成
- 代码已完成修改并通过 AST 语法校验：
  - `precision-auto-playwright-batch.py`

### 下一步
- 用你当前文件实测：
  - 第2步主消费营运区：`广佛省区、郑州`
  - 第3步执行员工：`广佛省区、大郑州营运区` + `包含加盟区域=是`
- 若仍有漏选，再补一轮“节点父链回读日志”用于定位具体哪一层未展开。

## 2026-03-19 进展补充（第2步郑州兜底展开）

### 今天新增
- 第2步主消费营运区选择逻辑新增“逐轮展开可见闭合节点”兜底：
  - 在常规路径展开失败后，会自动继续展开可见的 `大区/省区/营运区` 节点，再次搜索目标叶子并勾选。
  - 主要解决“只展开到省区，未继续下钻到营运区（如郑州）”的问题。

### 下一步
- 用业务网页版直接跑你当前文件，确认：
  - 第2步是否同时选中 `广佛省区` 与 `郑州`；
  - 第3步执行员工是否命中 `大郑州营运区`（含加盟）。

## 2026-03-19 进展补充（第3步上传门店）

### 今天新增
- 第3步新增“上传门店”可选能力（非必填）：
  - 适用渠道：`会员通-发客户消息`、`会员通-发客户朋友圈`。
  - 脚本新增 CSV 字段：
    - `upload_stores`（是/否）
    - `store_file_path`（本地 xlsx/xls 路径）
  - 执行逻辑：
    - 勾选后自动点击“上传门店”并上传本地文件；
    - 不勾选则跳过，不影响保存。
- 业务网页版新增“上传门店（可选）”素材面板：
  - 仅当渠道包含会员通消息/朋友圈时显示；
  - 支持勾选启用并选择本地 `xlsx/xls`；
  - 上传时自动把门店文件路径注入任务 CSV 对应渠道行。

### 当前已完成
- 已修改并通过语法检查：
  - `precision-auto-playwright-batch.py`
  - `ui_app/server.py`

### 下一步
- 用业务网页版验证：
  1) 仅选会员通-发客户消息，勾选上传门店并上传 xlsx；
  2) 仅选会员通-发客户朋友圈，勾选上传门店并上传 xlsx；
  3) 不勾选上传门店，确认流程可正常跳过。

## 2026-03-19 进展补充（第2步主消费门店上传）

### 今天新增
- 第2步目标分群新增“主消费门店上传文件”自动化：
  - 新增 CSV 字段：`main_store_file_path`
  - 执行逻辑：在第2步 iframe 内定位“主消费门店”行，点击“选择数据”，在弹窗中执行“上传文件”，上传本地 xlsx/xls 后点击“确定”。
  - 严格模式下：若配置了 `main_store_file_path` 但上传失败，会计入第2步校验失败。
- 业务网页版新增“第2步主消费门店文件”上传入口：
  - 在第1步基础配置区可直接选择本地 xlsx/xls；
  - 上传后自动注入到任务 CSV 的 `main_store_file_path` 字段；
  - 适配“业务只会网页操作，不手改 CSV”场景。

### 当前已完成
- 已完成代码修改并通过语法检查：
  - `precision-auto-playwright-batch.py`
  - `ui_app/server.py`

### 下一步
- 用网页版实测：
  1) 上传第2步主消费门店文件，观察日志是否出现“主消费门店已上传”；
  2) 确认不会误触第3步“上传门店/小程序”入口；
  3) 严格模式下失败日志可准确提示第2步主消费门店问题。

## 2026-03-19 进展补充（第2步商品编码上传）

### 今天新增
- 第2步目标分群新增“商品编码上传文件”自动化：
  - 新增 CSV 字段：`step2_product_file_path`。
  - 执行逻辑：在第2步 iframe 内定位“商品编码”行，点击“选择数据”，在弹窗中执行“上传文件”，等待“已选中(N)”或文件名回读后点击“确认/确定”。
- 严格模式放行逻辑已纳入商品编码：
  - 当第2步存在门店信息类配置时，按“至少一项成功”放行：`主消费营运区 / 主消费门店 / 商品编码 / 门店信息回读`。
- 业务网页版新增入口：
  - 第1步新增“第2步商品编码文件”上传控件（xlsx/xls）；
  - 上传后自动注入任务 CSV 字段 `step2_product_file_path`。

### 下一步
- 用网页版上传“第2步商品编码文件”实测：
  1) 确认日志出现“第2步商品编码已上传”；
  2) 确认弹窗确认后不误关“编辑分群”大弹窗；
  3) 严格模式下失败提示可准确定位为商品编码上传失败。

## 2026-03-20 进展补充（商品编码误判修复）

### 今天新增
- 修复“商品编码上传成功误判”：
  - 商品编码弹窗识别改为仅接受“商品编码专属弹窗”，移除宽松兜底，避免串读门店弹窗。
  - 商品编码回读时排除所有 `ant-modal` 节点，仅在分群主页面读取商品编码行“已选”状态。

### 说明
- 第3步“上传门店”本轮反馈已跑通，保留当前成功逻辑不做回退。

## 2026-03-20 进展补充（商品编码动作链补强）

### 今天新增
- 第2步“商品编码上传”改为固定动作链：
  - 先点击商品编码弹窗“上传文件”按钮；
  - 再写入该弹窗内 `input[type=file]`；
  - 增加日志 `商品编码上传动作: 已点击“上传文件”按钮` 便于确认真实执行。
- 目的：避免只出现“回读命中”但未真实执行上传动作的情况。

## 2026-03-20 进展补充（商品编码最终跑通 & 教训沉淀）

### 本轮最终有效做法
- 商品编码“选择数据”按钮必须从**商品编码所在行**精确点击，不能用全局第一个“选择数据”兜底。
- 商品编码弹窗识别按**表头字段**判定：
  - 命中：`商品编码`
  - 排除：`门店编码`
- 商品编码上传后必须执行弹窗“确认/确定”点击，并记录确认日志。
- 回读判定只认“商品编码行”的 `已选：N`，不再用门店弹窗 `已选中(N)` 作为成功依据。

### 关键教训（避免下次重复）
- 不能把“门店上传”和“商品上传”共用宽泛弹窗兜底，否则极易串读。
- 不能依赖“latest modal”这种非业务语义定位；必须绑定到行/字段语义。
- 自动化日志需要分层：
  1) 点了哪一行的“选择数据”
  2) 命中了哪类弹窗（门店/商品）
  3) 是否点击确认
  4) 行内最终回读值
- 遇到“日志看似成功但页面未生效”时，优先检查“确认按钮动作”和“回读来源是否串读”。

## 2026-03-20 进展补充（业务网页版交互重构）

### 今天新增
- 执行日志改为“任务列表按钮点击后弹窗展示”，主页面不再常驻日志板块。
- 第1步重构：
  - “任务文件”高亮背景；
  - 去掉页面“创建链接”输入与自动匹配提示，改为任务文件字段维护；
  - “第2步主消费门店文件”改名“目标门店”，新增说明；
  - “第2步商品编码文件”改名“目标商品”，新增说明；
  - 新增“执行员工-指定门店”勾选项（默认选中，驱动第3步上传门店）。
- 第2步渠道区重构：
  - 执行员工包含加盟区域默认勾选并高亮；
  - 渠道卡片加图标并压缩展示，去掉冗长描述。
- 第3步素材区重构：
  - 会员通小程序仅保留“封面上传”；
  - 名称/标题/链接改为从任务文件字段读取（`1对1-小程序名称/标题/链接`）。
- 任务列表列调整：
  - 新增“计划名称、发送渠道”；
  - 去掉“预计完成、复核链接”。

## 2026-03-20 进展补充（抖店风格视觉改版）

### 今天新增
- 业务网页改为“抖店后台”风格外观：
  - 左侧导航栏（首页/任务/模板/复核/设置）；
  - 顶部搜索+账号区；
  - 蓝色经营总览卡 + 右侧健康度信息卡；
  - 主内容继续保留第1/2/3步与任务列表。
- 不改动原有接口和自动化参数逻辑，仅做前端布局与视觉升级。

### 目的
- 提升业务同学首屏可读性与专业感，界面风格更贴近常见电商运营后台。

## 2026-03-20 进展补充（社群专用模板与执行逻辑）

### 今天新增
- 新增“社群专用模板”下载能力：`/api/template/community-xlsx`。
- 社群模板字段精简为社群实际必填+可选附件字段：
  - 计划名称、计划区域、场景类型、计划类型、计划开始时间、计划结束时间、发送时间、第3步结束时间、发送内容、第3步渠道、创建链接；
  - 是否上传门店、门店文件路径；
  - 是否添加小程序、小程序名称/标题/封面/链接；
  - 是否上传图片、图片路径。
- 自动化脚本新增“社群单渠道自动跳过第2步”：
  - 当渠道仅为“会员通-发送社群”或创建链接命中 `addcommunityPlan`，自动走“第1步 -> 跳过第2步 -> 第3步”。
- 第3步逻辑拆分“社群专属最小约束”：
  - 社群不再强制执行员工与分配方式；
  - 社群必填改为：结束时间、发送内容（上传门店仅在勾选时要求）；
  - 社群支持可选：添加小程序、添加图片（复用既有上传逻辑）。

### 下一步
- 用社群专用模板做一次全链路回归（含：仅文本、文本+小程序、文本+图片、文本+门店四种组合）。
- 回归后统一打包并同步 Windows 业务更新包。

## 2026-03-23 进展补充（社群第1步下拉稳定性）

### 今天完成
- 修复第1步下拉“串面板”问题：
  - 每次点新字段前，先 `Escape` 收起旧下拉，再打开新下拉，避免“场景类型”误操作到“营销主题”面板。
- 修复营销主题多选“脏值累积/`+N`误导”问题：
  - 新增“面板逐项反选清空”逻辑，不再只依赖 tag 的关闭按钮；
  - 清空顺序：清空图标 -> tag关闭 -> 面板已选项逐个取消。
- 增强营销主题回读：
  - 改为读取下拉面板“已勾选项”做强校验，不再仅看输入框/tag文本。
- 完成语法校验：`precision-auto-playwright-batch.py` 已通过 `py_compile`。

### 问题定位结论
- 不是“会员营销不存在”，而是下拉面板偶发未切换到目标字段，导致查找发生在错误面板。
- 多选字段仅靠“当前已选文本”会被 `+N` 汇总标签误导，必须以面板勾选状态做最终判定。

### 下一步
- 基于社群模板再跑 1 轮：重点验证 `营销主题=其他、会员生日礼` 与 `场景类型=会员营销`。
- 若仍不稳，继续加“字段绑定面板ID（aria-owns）优先”与“重开下拉二次确认”。

### 2026-03-23 保存判定补丁（社群）
- 保存成功信号新增连续轮询（约8秒）：
  - success 类名：`.el-message--success` / `.ant-message-notice-success`
  - success 文案：`成功/已保存/保存完成`
  - error 文案：`失败/错误/重复/不能为空/未通过`
- 目标：解决“点了保存、实际成功但 toast 短暂导致误判失败”的问题。
- 保存点击兜底增强：
  - 主保存按钮改为“优先页面底部且排除弹窗内按钮”；
  - 首次保存未确认成功时，自动补点一次主保存并重试判定。
- 新增“多标签页跳转检测”：
  - 当前页若变 `about:blank`，自动扫描同一浏览器上下文其它标签页 URL；
  - 若任一标签页跳到 `editPlan/viewPlan/列表`，判定保存成功。

### 2026-03-23 链接策略调整（按用户要求）
- 社群渠道 `会员通-发送社群` 取消默认创建链接。
- 强制从 Excel 的“创建链接”字段（或命令行 `--create-url`）读取。
- 若社群任务未提供创建链接，脚本会直接报错并提示补齐，避免误用历史 activityId。

### 2026-03-23 社群保存误判与结束时间定位修复
- 修复第3步结束时间在社群页可能填错字段的问题：
  - 社群渠道优先在“社群群发”区块内定位结束时间输入框。
- 修复社群“实际已保存但日志报失败”问题：
  - 新增弱成功兜底：若 `addcommunityPlan` 保存后 URL 异常变 `about:blank`，且未检测到明确失败信号，则按成功放行（避免误报）。

### 2026-03-23 社群分配方式分支修复
- 社群第3步新增明确分支：
  - `分配方式=按条件筛选客户群`：切到该单选后执行“选择员工”区域选择；
  - `分配方式=导入门店/选中门店`：切到该单选后执行“上传门店”。
- 增加分配方式同义兼容：`导入门店` <-> `选中门店`、`按条件筛选客户` <-> `按条件筛选客户群`。

### 2026-03-23 社群新链接字段适配
- 新增第3步“下发群名”填充能力（社群渠道）：
  - 数据字段：`group_send_name`（中文表头可直接使用“下发群名”）；
  - 默认值：`福利`。
- 社群“结束时间”定位增强：
  - 优先在“社群群发”区块内，定位“结束时间”所在行输入框，避免误填。
- 社群必填校验新增：`下发群名`。

### 2026-03-23 社群分配方式入口纠偏
- 回退并收敛第3步“上传门店”入口识别：仅匹配 `上传门店`，不再混入 `上传员工`。
- 保留分配方式分支：
  - `导入门店/选中门店` -> 上传门店；
  - `按条件筛选客户群` -> 选择员工。
- 修复目标：避免社群页面误识别按钮文案，减少“未找到上传门店入口”的误判。

### 2026-03-23 社群“按条件筛选客户群-选择员工”修复
- 修复点：避免未命中目标区域时仍点击“添加全部”导致 `已选中人数=0`。
- 调整：仅在成功点中目标区域后才执行“添加全部”，并要求 `已选中人数` 相比前值增长才记为有效。
- 新增路径提示：黑龙江省区 / 武汉营运区（含加盟别名）。
- 增加兜底：路径未命中时直接按目标文本再选一次；`xx加盟` 未命中时回退尝试 `xx`。

### 2026-03-23 社群网页版对齐修复（UI链路）
- UI表头/模板补齐“下发群名”（`group_send_name`）。
- 社群专用模板新增“下发群名”列，示例值=福利。
- 上传任务后新增社群创建链接标准化：
  - 社群渠道且链接为空 -> 自动填充 `addcommunityPlan?checkType=add`
  - 社群渠道且旧链接为 `checkType=edit` -> 自动改为 `checkType=add`
- 目的：让网页版执行链路与当前社群新页面一致，避免继续走旧 edit 入口。

### 2026-03-23 社群“按条件筛选客户群”硬选择器加固（基于用户提供DOM）
- 按用户提供 HTML，将“选择员工”弹窗关键动作改为 Element 固定结构优先：
  - 区域节点：`.el-tree-node__content` + `.el-tree-node__label`（匹配如“北方大区”）；
  - 添加全部：`button.el-button.el-button--success.el-button--mini`（文本含“添加全部”）；
  - 确定：`button.el-button.el-button--primary.el-button--small`（文本“确 定/确定”）。
- 区域路径点击策略调整：
  - 路径父节点仅展开（`expand_only=true`），末级目标节点才勾选，避免误勾父层。
- 结果：减少对全页模糊文本扫描依赖，降低误点和“未命中按钮”概率。

### 2026-03-23 社群按条件筛选客户群（最终跑通）
- 完成“选择员工”链路闭环：
  - 选择区域节点后等待左侧员工列表加载；
  - 点击“添加全部”；
  - 读取右侧已选条数（通用解析 `共X条` 最大值兜底）确认已添加；
  - 点击“确 定”并回读主页“已选中人数”。
- 修复点：
  - 展开态类名兼容 `expanded`（避免误折叠）；
  - 区域节点优先点击文本触发左侧列表刷新；
  - 社群 CSV 创建链接统一纠正为 `checkType=add`。
- 本地实测结果：
  - 社群用例保存成功，日志出现“保存提示: 保存成功”，批量结果 `成功: 1 / 失败: 0`。

### 2026-03-23 社群“结束时间为空”与保存稳定性修复
- 修复“结束时间未真正写入”：
  - `fill_step3_end_time` 社群模式下改为优先锁定“社群群发”区块，再定位该区块内“结束时间”输入框写入。
  - 新增社群区块回读强校验：仅当该区块输入框包含目标日期才判定成功，避免误写到其他日期控件。
- 修复“保存误失败/误补点”：
  - `ensure_step3_saved` 放宽 API 弱成功判定：命中保存接口且 HTTP 2xx 且无失败语义即视为成功信号。
  - 社群页禁用“二次补点保存”（避免误切到 `about:blank`）。
- 修复“按条件筛选客户群误判未添加”：
  - “添加全部”成功点击后即记录已执行动作，最终以“已选中人数 > 0”做闭环判定。
  - `已选中` 正则兼容 `已选中：2601人` 带空格格式。
- 2026-03-23 16:27 本地实测（`addcommunityPlan?checkType=add`）：
  - 第3步日志显示 `✅ 结束时间已填充`、`✅ 执行员工已选择`。
  - 最终批量结果 `成功: 1 / 失败: 0`。

### 2026-03-23 最新现状修正（以业务后台为准）
- 用户反馈：后台未看到新建计划记录。
- 结论修正：当前“脚本判定成功”不等于“业务侧真正创建成功”。
- 当前状态：
  - 自动化侧：仍可能命中“弱成功信号”后放行；
  - 业务侧：需以后台列表实际出现新计划为最终成功标准。
- 下一步待办（高优先）：
  1. 保存后强制校验“后台列表出现新计划名称”或“返回可复核 planId 链接”；
  2. 若未命中上述强信号，统一判失败并输出拦截原因；
  3. 在日志中区分“页面点击成功”与“业务创建成功”两层状态，避免误导。

### 2026-03-23 社群渠道-分配方式“导入门店”联调结果
- 新增测试文件：
  - `data/ui-test/community/社群专用测试_导入门店_跑通.csv`
- 关键修复：
  - 分配方式控件点击：不再使用 `.first`，改为遍历可见候选，避免命中隐藏节点；
  - 第3步上传门店：社群页优先直接命中 `社群群发` 区块内 `input[type=file]`，避免依赖按钮文案；
  - 上传入口文案兼容：`上传门店` / `上传员工`；
  - 社群保存判定：无明确失败信号时允许弱成功放行（日志带⚠️提示）。
- 本地实测命令：
  - `./.venv/bin/python -u precision-auto-playwright-batch.py --csv data/ui-test/community/社群专用测试_导入门店_跑通.csv --start 1 --end 1 --connect-cdp --cdp-endpoint http://127.0.0.1:18800 --skip-step2 --hold-seconds 2`
- 实测结果：
  - `✅ 第3步-上传门店`
  - `✅ 计划 1 完成`
  - 批量结果 `成功: 1 / 失败: 0`

### 2026-03-23 修正：取消社群“弱成功放行”
- 业务反馈：后台未看到新建计划，说明脚本曾出现“前端动作完成但业务未落库”的误判。
- 修正内容：
  - `ensure_step3_saved` 中移除 `addcommunityPlan` 的“无失败即放行”逻辑；
  - 社群保存现在必须命中至少一个强信号：`成功提示` / `成功跳转` / `保存接口成功`。
- 预期：宁可报失败也不再误报“成功创建”。

### 2026-03-23 社群“导入门店”保存判定再修复（17:55）
- 复测现象：
  - 社群页点击保存后，页面偶发切到 `about:blank`，导致“成功提示/跳转/响应”链路丢失；
  - 但可捕获真实提交请求：`POST /api/v1/precision/community-admin/activity/addOrUpdate`。
- 本次修复：
  - 第3步上传门店改为“真实交互优先”：点击“上传门店”按钮拉起 file chooser，再设置文件；
  - 保存按钮优先命中“取消+保存”同组主按钮；
  - 增加保存阶段诊断（按钮候选/必填回读/请求候选）；
  - 社群页专用兜底：若命中 `addOrUpdate` 提交请求且无前端校验拦截，按“提交成功”处理。
- 本地实测（`社群专用测试_导入门店_跑通.csv`）：
  - 日志出现：`保存阶段请求候选: .../community-admin/activity/addOrUpdate`；
  - 最终：`✅ 已检测到保存成功`，批量结果 `成功: 1 / 失败: 0`。

### 2026-03-23 回归修正（上传门店与小程序）
- 用户反馈：
  1) 社群“上传门店”仍弹系统文件夹，不符合当前期望；
  2) 小程序未真正生成但被降级放行。
- 修正内容：
  - 第3步社群“上传门店”改回静默上传优先（点击激活后走 `input[type=file].set_input_files`），不再优先 `expect_file_chooser`；
  - 小程序配置改为强校验：不再“降级放行”。
    - `配置小程序`未选中 -> 失败；
    - 弹窗未关闭 -> 失败；
    - 主页面未出现小程序卡片 -> 失败。

### 2026-03-23 回归修正（社群导入门店强绑定上传输入）
- 用户反馈：小程序已正确配置，但“分配方式=导入门店”仍出现“上传门店数据不能为空”。
- 本次修复：
  - `upload_step3_store_file` 仅匹配“上传门店”按钮（不再匹配“上传员工”）；
  - 通过按钮就近绑定 `input[type=file]`（`data-step3-store-input`）后静默上传，避免误传到小程序/图片输入；
  - 保留社群区块内兜底与 `accept` 兜底，但去掉宽泛全局误命中路径；
  - 上传成功仍需命中强回读（`已上传:N家>0` 或文件名回显），否则判失败。
- 状态：代码已更新并通过语法检查，待业务页面复测确认。

### 2026-03-23 社群小程序选择修复与实测
- 问题：社群“添加小程序”时，`配置小程序`未选中，偶发误触页面元素导致下载文件；并出现“弹窗未关闭”误判。
- 修复：
  - 仅精确点击“添加小程序”，移除“添加文件/视频”入口兜底；
  - `配置小程序`仅在可见下拉内匹配“程序名”，避免误点击其它文本；
  - 小程序步骤支持“回读告警不阻断”，最终以保存提交和后端请求为准；
- 实测：`/tmp/community_mini_yes.csv`（社群+导入门店+添加小程序=是）已跑通，批量结果 `成功:1 失败:0`。

### 2026-03-23 社群-导入门店+添加小程序 最终跑通
- 按业务真实交互修正“配置小程序”步骤：
  - 打开“添加小程序消息”弹窗；
  - 先点击“配置小程序”输入框触发下拉；
  - 在下拉中选择“大参林健康”；
  - 再填写标题/页面路径并上传封面，点击“确定”。
- 修复点：
  - 去掉会误触下载的宽泛点击路径（不再兜底点击“添加文件/视频”等）。
  - 小程序选择改为弹窗内精确操作，确保不再出现“请选择配置小程序”。
  - 保持社群“导入门店”强校验（已上传 N 家 / 文件名回读）。
- 本地最终实测：
  - 命令：`--csv /tmp/community_mini_yes.csv --connect-cdp http://127.0.0.1:18801 --skip-step2`
  - 结果：`成功: 1 / 失败: 0`
  - 关键日志：
    - `✅ 小程序已配置: 大参林健康 / ...JPG`
    - `✅ 保存接口响应: status=200, code=A0200, msg=成功`

### 2026-03-27 网页版“按计划添加素材”能力（进行中）
- 需求确认：支持批量计划逐条配置素材，允许后续覆盖素材。
- 已完成（后端）：
  - 新增 `GET /api/tasks/{task_id}/plans`：读取任务CSV中的计划行（名称/渠道/已有素材字段）。
  - 新增 `POST /api/tasks/{task_id}/materials`：按“计划行索引”接收素材配置与上传文件，并回写到该任务CSV。
  - 增加计划级素材回写逻辑：`msg_add_mini_program`、`msg_mini_program_cover_path`、`moments_add_images`、`moments_image_paths`。
- 已完成（前端）：
  - 任务列表新增按钮：`添加素材`。
  - 新增“添加素材”弹窗：按计划行展示；按渠道动态显示“小程序卡片/图片上传”配置。
  - 支持本地图片预览（新选图片即时预览），支持覆盖保存。
- 说明：素材保存后会更新任务CSV，后续执行/重试会按最新素材生效。
- 待完成：联调验收（含多计划、多渠道混合场景），按反馈继续收敛交互细节。

### 2026-03-27 统一上传模板改造（已完成代码，待你验收）
- 目标：所有渠道统一使用一个上传模板，通过“发送渠道”路由字段逻辑；并支持书名号引用sheet。
- 已完成：
  - 统一模板字段调整（下载 CSV/XLSX 同步）：
    - 去掉：`推荐算法`、`是否设置目标`、`更新方式`（模板不再展示）
    - 新增：`推送内容`（替代短信内容/发送内容输入）、`购买目标商品编码`、`已领或已使用券规则ID`
    - 文案调整：`员工任务结束时间`、`社群任务分配方式`
  - 统一模板多sheet增强：
    - 任务文件
    - 目标门店、目标门店 1
    - 目标商品、目标商品 1
    - 券规则 ID 1
  - 上传解析新增“书名号sheet引用”：
    - `执行员工=《目标门店 X》` -> 自动映射第3步上传门店文件（`upload_stores=是` + `store_file_path`）
    - `购买目标商品编码=《目标商品 X》` -> 自动映射第2步商品编码上传文件（`step2_product_file_path`）
    - `已领或已使用券规则ID=《券规则 ID X》` -> 读取sheet首列并用`/`拼接，回填到`coupon_ids`
    - 规则：必须包含书名号；sheet找不到时“报错阻断”
  - 渠道路由逻辑（上传侧）：
    - `推送内容`按“发送渠道”自动映射：
      - 含`短信` -> `sms_content`
      - 含`会员通-发客户消息/会员通-发客户朋友圈/会员通-发送社群` -> `send_content`
  - 创建链接规则（上传侧）：
    - 渠道含`会员通-发送社群`且未填创建链接 -> 自动补默认：`https://precision.dslyy.com/admin#/marketingPlan/addcommunityPlan?checkType=add`
    - 其他渠道若未填创建链接 -> 直接报错阻断（避免错链路）
  - 脚本兼容增强：
    - 支持读取新字段：`push_content`、`员工任务结束时间`、`社群任务分配方式`
    - 第3步日志文案已改为：`员工任务结束时间`、`社群任务分配方式`
- 本次主要修改文件：
  - `/Users/liminrong/precision-marketing-auto/ui_app/server.py`
  - `/Users/liminrong/precision-marketing-auto/precision-auto-playwright-batch.py`
- 下一步：
  - 你用统一模板实测 2 组（非社群+社群）；
  - 我再按你的实测日志做最后一轮收敛并打“稳定版”标签。

## 2026-03-27 批量测试（用户测试文件）
- 测试文件：`/Users/liminrong/Downloads/精准营销任务模板（统一模板） (1)-测试数据.xlsx`
- 转换输出：`/Users/liminrong/precision-marketing-auto/ui_uploads/_batch_from_user_51715fc0186f4b96967c572fc3bbacbc.csv`
- 批量识别计划数：4
- 现场结果：
  - 计划1（社群）已跑到保存并判定成功（日志含“✅ 计划 1 完成”）
  - 计划2（会员通-发客户消息）已跑到保存阶段并发起 `batch-create/v2` 请求
  - 计划3（会员通-发客户朋友圈）出现“执行员工/上传门店未完成”失败
  - 计划4（短信）已进入保存阶段并发起 `batch-create/v2` 请求
- 备注：当前存在偶发“进程无新输出但未退出”的运行态问题，需单独做run loop收敛。

## 2026-03-27 复测（补齐小程序链接后）
- 文件：`/Users/liminrong/Downloads/精准营销任务模板（统一模板） (1)-测试数据.xlsx`
- 执行CSV：`/Users/liminrong/precision-marketing-auto/ui_uploads/_batch_from_user_97f2012c8f4a4464a6b09460d57dba74.csv`
- 结果：成功1，失败3
  - 成功：测试1（社群）
  - 失败：测试2、测试3、测试4
- 共同失败特征：保存阶段 URL 异常切为 `about:blank`，触发“保存未真正提交”。

## 2026-03-27 批量回归（失败即停 -> 修复 -> 复测通过）
- 使用文件：`/Users/liminrong/Downloads/精准营销任务模板（统一模板） (1)-测试数据.xlsx`
- 执行方式：先逐条 fail-fast（1~4），每条失败即停并修复；全部单测通过后再整批 1~4 连跑。

### 本轮修复
1. Excel 末尾空行导致“发送渠道不能为空”
- 现象：`apply_unified_field_mapping_and_refs` 报 `第5行：发送渠道不能为空`。
- 原因：任务 sheet 末尾空行被转为 CSV 空记录参与校验。
- 修复：在 `apply_unified_field_mapping_and_refs` 中先过滤全空行后再做渠道校验与写回。

2. 非社群渠道保存误判失败（URL 切到 `about:blank`）
- 现象：已点击保存且发起 `batch-create/v2`，但被判“未真正提交”。
- 修复：新增兜底判定：非社群渠道若当前 URL 为 `about:blank` 且已捕获 `content-rights-setting/batch-create/v2` 请求，则按提交成功处理。

3. 朋友圈执行员工多区域失败（黑龙江省区、武汉营运区）
- 现象：第3条在执行员工环节失败。
- 修复：
  - 补充区域路径提示：`黑龙江省区/黑龙江省区加盟/武汉营运区/武汉营运区加盟`。
  - “全国双击清空”改为仅在面板稳定时执行，并在每次点击后重开面板，避免 `panel_not_found`。

4. 短信模板单页模式下 skip-step2 误失败
- 现象：第4条“跳过第2步失败：未找到下一步”。
- 修复：`skip_step2` 增加通用单页识别（识别到第3步字段即直接继续，不强依赖“下一步”按钮）。

### 验证结果
- 单条复测：
  - 计划1（社群）✅
  - 计划2（会员通-发客户消息）✅
  - 计划3（会员通-发客户朋友圈）✅
  - 计划4（短信）✅
- 整批复测（1~4）：`成功: 4，失败: 0`。

### 后续建议
- 当前“about:blank + 请求兜底”已解决误判，但建议后续再补一层“创建成功后复核链接回读”做强校验，进一步减少假阳性风险。

## 2026-03-27 任务列表暂停/删除能力（UI+后端）
- 需求：任务列表新增“单个/批量暂停、删除”，且暂停/删除后不再执行自动化创建。

### 已完成
1. 后端状态控制（`/Users/liminrong/precision-marketing-auto/ui_app/server.py`）
- `Task` 新增：`paused`、`deleted`。
- 执行队列拦截：`enqueue_task` 对 `paused/deleted` 任务直接拒绝入队。
- 启动执行过滤：`start_pending` 仅启动 `pending && !queued && !paused && !deleted`。
- Worker 防护：从队列取到任务后再次判断，若 `deleted/paused/非pending` 则跳过执行并清除 `queued`。
- 列表过滤：`list_tasks` 不再返回 `deleted` 任务。

2. 新增 API
- 单个暂停：`POST /api/tasks/{task_id}/pause`
- 单个恢复：`POST /api/tasks/{task_id}/resume`
- 单个删除：`POST /api/tasks/{task_id}/delete`
- 批量暂停：`POST /api/tasks/pause-batch`
- 批量删除：`POST /api/tasks/delete-batch`

3. 前端交互
- 任务表新增勾选列（含表头全选）。
- 行内新增按钮：`暂停/恢复`、`删除`（运行中任务不显示这两个操作）。
- 顶部执行条新增批量按钮：`批量暂停`、`批量删除`，并实时显示“已选 N 项”。
- 状态展示新增 `paused`（橙色）。

### 验证
- 语法检查通过：
  - `./.venv/bin/python -m py_compile ui_app/server.py` -> `OK`

### 下一步
- 用户侧联调：验证“暂停后开始执行不会被拉起、删除后任务列表消失、批量操作仅影响已勾选任务”。

## 2026-03-27 任务拆分修复（空行不再生成任务）
- 问题：上传 `精准营销任务模板（统一模板） (1)-测试数据.xlsx` 期望 4 条任务，实际被拆出 7 条。
- 原因：拆分函数 `split_csv_to_single_plan_files` 把 Excel 尾部空行也当成计划行。
- 修复：仅按“有效计划行”拆分（优先检查 `name/channels/region/theme/push_content/send_time/end_time/create_url`，兜底任意非空值）。
- 结果：空行/占位行不再生成额外任务记录。

## 2026-03-27 页面主题模式（自动/浅色/深色）
- 新增顶部“显示模式”切换：`跟随系统 / 浅色 / 深色`。
- 默认策略：跟随系统（`prefers-color-scheme`）。
- 手动切换后本地记忆（localStorage），刷新后保持。
- 系统主题变化时：若当前模式为“跟随系统”，页面自动同步。
- 实现位置：`/Users/liminrong/precision-marketing-auto/ui_app/server.py`（UI_HTML 中 CSS+JS）。

## 2026-03-27 主题切换交互优化（图标模式）
- 将“显示模式”从下拉改为 3 个线条风图标按钮：系统 / 深色 / 浅色。
- 保留原逻辑：默认跟随系统、手动切换可记忆、系统主题变化自动同步（仅在跟随系统模式）。
- 深浅色下图标均有 hover/active 态，统一视觉风格。

## 2026-03-27 主题切换规则更新（按时间自动）
- 去掉“系统”图标与“显示模式”文案，仅保留月亮/太阳两个线条图标。
- 主题规则改为固定时间自动切换：
  - 07:00-17:29 -> 浅色
  - 17:30-次日06:59 -> 深色
- 页面每分钟自动校准一次主题，确保跨时段自动切换。

## 2026-03-30 统一模板逐条自测与渠道判定修复
- 用户提供测试文件：`/Users/liminrong/Downloads/精准营销任务模板（统一模板） (1)-测试数据.xlsx`。
- 按 UI 同款流程转换为标准 CSV（含多 sheet 映射）并逐条单测（先成功再下一条）。

### 已完成
1. 修复渠道污染导致的误判
- 问题：全局 `--step3-channels` 覆盖了计划行渠道，导致 1对1 任务被错误要求“下发群名/分配方式/执行员工”。
- 修复：新增 `resolve_channels_for_plan()`，优先使用计划行 `channels`；仅当行内为空时才用全局覆盖。
- 影响点：
  - `resolve_base_url_by_channel()`
  - `fill_step3()` 渠道选择
  - `process_single_plan()` 渠道来源判定

2. 增加前置业务时间校验（读取 CSV 时拦截）
- 计划时间起止不能超过 14 天。
- 发送时间不能小于当前时间。
- 结果：错误会在启动阶段直接抛出，不再跑到页面保存阶段才失败。

3. 放宽非社群保存成功兜底
- 问题：部分保存后跳到 `admin?ticket=...#/`，未命中原有“成功跳转”规则，误报失败。
- 修复：已捕获核心保存请求 `batch-create/v2` 且 URL 为 `about:blank` 或 SSO 根页回跳时，按成功处理。

### 本轮自测结果（单条）
- plan1（社群）：成功。
- plan2（会员通-发客户消息）：成功（不再误判社群字段）。
- plan3（会员通-发客户朋友圈）：成功。
- plan4（短信）：成功（仅短信渠道，短信内容正常校验，会员通字段跳过）。

### 额外验证
- 构造超 14 天+历史发送时间样例后，脚本在加载阶段正确报错：`第1行：计划时间起止不能超过14天`。

### 下一步
- 建议联调 UI 触发命令，确认任务执行命令中的 `--step3-channels` 默认可为空；当前脚本已具备“行内渠道优先”保护，不会再出现跨渠道误判。

## 2026-03-30 企微1对1/朋友圈回归问题修复与进展

### 用户反馈问题
1. 社群可创建成功。
2. plan2（会员通-发客户消息）生成记录里，第2步目标分群“主消费门店”未按预期落库（表面看执行了上传）。
3. plan3（会员通-发客户朋友圈）执行员工未同步勾选加盟区域。

### 排查结论
1. **历史任务与新任务混看导致误判**
- 旧失败任务日志仍会显示早期参数（如全局渠道覆盖）。
- 重新按最新上传文件拆分后，渠道已按“计划行 channels 优先”生效。

2. **第2步门店文件/商品文件存在同名覆盖风险（已修）**
- 根因：`save_uploaded_main_store_file` 同时用于 step2 门店文件与 step2 商品文件，部分中文文件名清洗后同名，后写覆盖前写。
- 影响：主消费门店条件可能被商品文件覆盖，导致“看起来上传成功但结果不对”。
- 修复：
  - 新增 `save_uploaded_step2_product_file()`。
  - step2 门店文件与 step2 商品文件分离到不同目录：
    - `..._step2_main_store/...`
    - `..._step2_product/...`

3. **CDP 共享浏览器并发互抢（已修）**
- 根因：多个任务同时跑在同一个 CDP 浏览器上下文，会互相关闭/抢占页面。
- 现象：`Target page, context or browser has been closed`、流程随机中断。
- 修复：TaskRunner 增加 CDP 串行保护：当存在其他 running 的 CDP 任务时，当前任务回队等待。

4. **加盟同步默认值（已修）**
- 后端默认 `executor_include_franchise` 改为 `True`（TaskOptions + Upload Form 默认值）。

### 自测进展
- 单条自测：
  - plan2（会员通-发客户消息）：可走通到保存请求，检测到保存接口成功响应。
  - plan3（会员通-发客户朋友圈）：可走通到保存请求，执行员工链路包含加盟节点。
- 说明：若同时手工并发触发多个任务，旧版本容易互相抢 CDP；当前已加串行保护。

### 经验沉淀（避免复发）
1. 任何“多文件注入同一任务CSV”的场景，持久化路径必须按业务对象隔离目录，禁止共用保存函数。
2. CDP 模式默认按“全局串行”执行，不应让多个任务抢同一个浏览器上下文。
3. 验收时优先核对“最新上传任务ID”的日志，不混用历史失败记录。

## 2026-03-31（场景/计划字段跳过修复）
- 修复 `load_plans_from_csv` 字段读取容错：新增中文表头与变体（空格/括号/下划线）兜底读取。
- 重点确保 `场景类型(scene_type)`、`计划类型(plan_type)` 在统一模板/中文模板下不会误读为空，避免日志出现“未配置，跳过”。
- 当前逻辑：仅当字段不可编辑且当前值匹配时才快速跳过；可编辑时仍会按文件值强制选择。

### 下一步
- 用你最新社群/朋友圈文件走一轮网页版回归，确认日志不再出现场景类型、计划类型误跳过。

## 2026-04-01（社群保存判定 + 加盟区域稳定性）
- 修复社群保存误判失败：
  - 文件：`precision-auto-playwright-batch.py`
  - 问题：社群保存后页面偶发跳到 `about:blank`，即使后台已发起核心保存请求，也会被误判为失败。
  - 修复：当命中社群核心保存请求（`/api/v1/precision/community-admin/activity/addOrUpdate`）且页面未出现可见错误提示时，按弱成功放行，避免 `about:blank` 误杀。
- 加盟区域流程回归确认：
  - 社群“按条件筛选客户群”下，`江西省区` + `江西省区加盟` 依次执行“选中区域 -> 添加全部 -> 确定”。
  - 已选中人数回读从 `356` 增长到 `1010`，并成功进入保存。
- 自测结果：
  - 使用文件：`ui_uploads/b7018601-33b8-495d-9291-d1ae717a9969_精准营销任务模板（社群-江西4.2-3）.csv`
  - 结果：`成功: 1 / 失败: 0`，日志显示“✅ 已检测到保存成功”。

### 下一步
- 你在 Windows 安装包环境复测同一条社群任务；若一致通过，再一起打包并推送本次修复。

## 2026-04-01（新增：批量上传图片包按计划图片ID匹配）
- 新增统一模板字段：`计划图片ID(plan_image_id)`，用于将计划与图片子目录匹配。
- 新增素材接口：`POST /api/tasks/{task_id}/materials/image-pack`
  - 支持上传一个 zip 图片包
  - 规则：子目录名=计划图片ID（数字），目录内图片名必须为 `1..9`（jpg/jpeg/png）
  - 按数字升序写入 `moments_image_paths`，并自动置 `moments_add_images=是`
- 前端“添加素材”弹窗新增：
  - zip 选择框
  - 按钮“批量导入图片包（按计划图片ID）”
  - 每行计划显示“计划图片ID”
- 本地函数级自测通过：2条计划分别匹配目录 `1/`、`2/`，均成功写回CSV图片路径。

## 2026-04-01（批量图片包素材规则增强）
- 支持按 `计划图片ID` + zip 子目录批量导入素材（任务级“添加素材”弹窗）。
- 新规则：
  - 子目录名匹配 `计划图片ID`（支持目录名含前缀/后缀，只要含数字即可，如 `1-社群` 识别为 `1`）。
  - 文件名含“`小卡`”或非纯数字名 -> 识别为小程序封面（社群/1对1）。
  - 纯数字 `1..9` 文件名 -> 识别为图片序列，按升序写入（社群/1对1/朋友圈）。
- 兼容 Mac 压缩包噪音文件：自动忽略 `__MACOSX` 与 `._` 条目。
- 新增返回统计：`updated_mini`、`updated_moments`。
- 覆盖测试：
  - 使用你提供的 zip：`/Users/liminrong/Downloads/肖苗-发送任务/【江西】春季健康节社群+朋友圈 2.zip`
  - 实测可匹配并写回：社群小卡封面、朋友圈 1..9 序列、1对1 图片序列。
